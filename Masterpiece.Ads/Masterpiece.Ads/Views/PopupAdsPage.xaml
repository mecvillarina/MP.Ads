<?xml version="1.0" encoding="utf-8" ?>
<views:MobileContentPageBase x:Class="Masterpiece.Ads.Core.Views.PopupAdsPage"
                             xmlns="http://xamarin.com/schemas/2014/forms"
                             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                             xmlns:prism="http://prismlibrary.com"
                             xmlns:views="clr-namespace:Masterpiece.Ads.Core.Views"
                             prism:ViewModelLocator.AutowireViewModel="True"
                             BackgroundColor="{StaticResource SecondaryBackgroundColor}"
                             ControlTemplate="{StaticResource MasterDetailMainPageControlTemplate}">
    <StackLayout Padding="25">
        <Button Command="{Binding DisplayInterstitialAdCommand}"
                Text="Display Interstitial Ad" />

        <Button Command="{Binding DisplayInterstitialVideoAdCommand}"
                Text="Display Interstitial Video Ad" />

        <Button Command="{Binding DisplayRewardedVideoAdCommand}"
                Text="Display Rewarded Video Ad" />
    </StackLayout>
</views:MobileContentPageBase>